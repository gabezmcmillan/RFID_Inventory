# Secrets
credentials.json
# Per-machine config with secrets (mistral_api_key, sync_token);
# copy settings.example.ini to settings.ini and fill it in locally.
settings.ini

# Local database
*.db

# Scanned bill-of-lading PDFs
scans/

# Python
__pycache__/
*.pyc
.venv/
venv/

# PyInstaller output
build/
dist/

# Cloud app deploy bundle (generated; see apps/cloud/README.md)
apps/cloud/deploy.zip
# Build-time copy of packages/contract (made by the vercel.json buildCommand;
# must never be committed -- a stale copy would shadow the real package)
apps/cloud/contract/

# OS
.DS_Store

# Cursor plan files (local working notes; rules stay tracked)
.cursor/plans/
test_intake.py
