/** Sweep & Count route — renders the {@link SweepScreen}. */

import { SweepScreen } from "../src/screens/SweepScreen";

export default function SweepRoute(): React.ReactNode {
  return <SweepScreen />;
}
