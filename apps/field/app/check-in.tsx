/** Check In route — renders the {@link CheckInScreen}. */

import { CheckInScreen } from "../src/screens/checkin/CheckInScreen";

export default function CheckInRoute(): React.ReactNode {
  return <CheckInScreen />;
}
