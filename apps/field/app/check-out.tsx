/** Check Out route — renders the {@link CheckOutScreen}. */

import { CheckOutScreen } from "../src/screens/checkout/CheckOutScreen";

export default function CheckOutRoute(): React.ReactNode {
  return <CheckOutScreen />;
}
