/** Warehouse browse route — renders the {@link WarehouseScreen}. */

import { WarehouseScreen } from "../src/screens/warehouse/WarehouseScreen";

export default function WarehouseRoute(): React.ReactNode {
  return <WarehouseScreen />;
}
