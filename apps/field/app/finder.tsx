/** Find a Tag route — renders the {@link FinderScreen} (expects ?epc=…). */

import { FinderScreen } from "../src/screens/FinderScreen";

export default function FinderRoute(): React.ReactNode {
  return <FinderScreen />;
}
