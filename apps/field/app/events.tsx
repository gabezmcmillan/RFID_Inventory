/** Event Log route — renders the {@link EventsScreen}. */

import { EventsScreen } from "../src/screens/EventsScreen";

export default function EventsRoute(): React.ReactNode {
  return <EventsScreen />;
}
