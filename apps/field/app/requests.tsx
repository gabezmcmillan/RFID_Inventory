/**
 * Requests route — renders the {@link RequestsScreen}.
 */

import { RequestsScreen } from "../src/screens/requests/RequestsScreen";

export default function RequestsRoute(): React.ReactNode {
  return <RequestsScreen />;
}
