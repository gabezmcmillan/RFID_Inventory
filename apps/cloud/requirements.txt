# The cloud app's dependency manifest, used by Vercel and local runs alike.
# (There is deliberately no pyproject.toml here: Vercel would prefer it over
# this file.)
#
# The shared sync contract (packages/contract) is NOT listed here -- Vercel's
# builder stages this file into a scratch directory before resolving it, so
# relative paths break. Instead the buildCommand in vercel.json copies the
# package into the app root at deploy time, and local runs install it
# explicitly:  pip install -r requirements.txt ../../packages/contract
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
# psycopg v3 with bundled libpq binaries (no system packages needed).
psycopg[binary]>=3.1
