pyserial>=3.5
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pypdf>=4.0
# Cloud sync worker (sync.py) -- outbound HTTPS to the cloud app.
requests>=2.31
# USB label printing via the Windows print spooler (printer.py); the network
# (TCP 9100) path needs nothing extra. Not installed on macOS.
pywin32>=306; sys_platform == "win32"
# The shared sync contract (see packages/contract). Resolved relative to the
# install CWD, so run pip from this directory -- build-windows.bat already
# does. PyInstaller bundles it into the exe like any installed package.
../../packages/contract
