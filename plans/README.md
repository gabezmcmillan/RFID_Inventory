# Implementation Plans — Expo Rewrite of the RFID Inventory System

Generated by the improve skill (plan variant) on 2026-07-22, at commit `79443fb`.

## What is being built

A ground-up replacement of the warehouse PC app (`apps/warehouse/`, Python/FastAPI/
PyInstaller) and the Vercel cloud mirror (`apps/cloud/`) with:

- **`apps/field/`** — an Expo (React Native) iPhone app. The phone clips to the
  Vulcan RFID Indium sled (Bluetooth, TSL ASCII 2.0 protocol) and replaces the
  warehouse PC entirely: Check In, Check Out, Sweep & Count, Find a Tag,
  Warehouse browse, Requests. BOL capture uses the phone camera instead of the
  Epson ES-50. Labels still print to the Zebra ZD621R over TCP/9100.
- **`apps/web/`** — a Next.js app on Vercel replacing `apps/cloud/`: jobsite
  stock browsing + cart requests + order status + tag QR pages, plus an
  office-facing warehouse browse, behind auth.
- **`packages/domain/`** — a TypeScript package holding the SQL schema, domain
  constants (ported from `apps/warehouse/config.py`), and every repository
  function (ported from `apps/warehouse/db.py`). It runs against a minimal
  `SqlDatabase` interface so the same code runs on-device (Turso React Native),
  in Node tests, and in the importer.
- **Data**: one Turso database. The field app uses
  `@tursodatabase/sync-react-native` (local-first reads/writes, `push()`/`pull()`);
  the web app reads/writes the same database over Turso's remote driver. This
  deletes the entire custom sync layer (`sync.py`, `packages/contract`, the
  mirror logic in `apps/cloud/db.py`) once cutover completes.

The existing Python apps stay untouched and running until plan 010's cutover.
Every plan is written so its verification passes **without hardware and without
Turso cloud credentials** (simulated reader, fake printer TCP server, local-only
database files).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Scaffold the TypeScript monorepo (field app, web app, domain package) | P1 | S | — | DONE |
| 002 | Domain package: schema, constants, repositories, importer | P1 | L | 001 | DONE |
| 003 | Reader module: TSL ASCII 2.0 over Bluetooth + simulated reader | P1 | L | 001 | DONE |
| 004 | Check In (scan path): arm shipment, trigger intake, amend, vendors, notes | P1 | M | 002, 003 | DONE |
| 005 | Label printing: ZPL over TCP, EPC minting, print-path intake | P1 | M | 004 | DONE |
| 006 | Check Out, Sweep & Count, Warehouse browse, Find a Tag, Admin | P1 | L | 004 | DONE |
| 007 | BOL capture with the camera + OCR field extraction | P1 | L | 004 | DONE |
| 008 | Requests: badge, staging, fulfill, decline | P1 | M | 006 | DONE |
| 009 | Web app: jobsite stock, cart, orders, tag pages, office browse, auth | P1 | L | 002 | IN PROGRESS — halted — auth will use Better Auth instead of Auth.js |
| 010 | Turso sync wiring, ops (EAS/Sentry), data import + cutover runbook | P1 | M | all | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Dependency notes

- **002 before every feature plan**: all repositories and types live in
  `packages/domain`; UI plans only wire screens to them.
- **003 before 004**: check-in consumes reader events. 004 is testable with the
  simulated reader that 003 ships, so hardware is never a blocker.
- **004 before 005/006/007**: those plans extend intake/checkout flows and the
  check-in screen that 004 creates.
- **006 before 008**: request fulfillment reuses the checkout staging flow.
- **009 only needs 002** (repositories + schema); it can run in parallel with
  003–008.
- **010 is last**: it turns on real sync, imports production `inventory.db`,
  and retires the Python apps. Nothing before it touches production data.

## Standing decisions (do not re-litigate in individual plans)

- **Same schema as `apps/warehouse/db.py`**, minus `sync_state` bookkeeping that
  the custom sync needed (a `local_meta` key/value table remains for the EPC
  serial and device id). Keeping the schema 1:1 makes the importer a straight
  copy and lets domain logic port line-for-line.
- **Multi-writer discipline**: the field app writes tags/events/vendors/
  notes/bol_docs and *updates* requests; the web app only *inserts* requests.
  Disjoint writes avoid sync conflicts.
- **EPC minting becomes device-safe**: EPC = 8-hex prefix (`42473031`) +
  2-hex device id + 14-hex per-device serial (was: 16-hex global serial).
  Two phones minting offline can no longer collide.
- **BOL documents are images/PDFs in blob storage** (upload queued while
  offline); the `bol_docs` row keeps `filename` and gains `storage_url`.
- **Everything testable in Node**: `packages/domain` never imports React
  Native. On-device it receives the Turso RN connection; in tests it receives
  `@tursodatabase/database` (Node).
- **Drizzle ORM is the data-access standard (2026-07-22)**: `packages/domain`
  uses `drizzle-orm@rc` / `drizzle-kit@rc` over the Turso drivers. The schema is
  Drizzle `sqliteTable` definitions (source of truth), row types are inferred,
  and migrations are generated by `drizzle-kit` and applied by Drizzle's
  migrator. Plans 003–010 executors must use Drizzle patterns (builder API for
  reads/writes, `db.run(sql\`...\`)` for raw statements, and the `sql` template
  operator with typed result mapping for complex aggregates) — no hand-rolled
  SQL seam.
- **Auth: Better Auth, not Auth.js/NextAuth (2026-07-22, operator decision;
  concretized 2026-07-23 from the `effectivly` reference repo)**: plan 009
  halted before its auth step. Auth uses [Better Auth](https://better-auth.com)
  `1.6.18` (same version as `effectivly`), following that repo's house style:
  the server instance lives in `apps/web/src/lib/auth.ts` (a `createAuth()`
  factory over `betterAuth()`), mounted as a catch-all at
  `app/api/auth/[...all]/route.ts` via `toNextJsHandler` from
  `better-auth/next-js`; the browser client is `createAuthClient()` from
  `better-auth/react` in `apps/web/src/lib/auth-client.ts`. Per the Better Auth
  guide, `secret`/`baseURL` are not set in config — Better Auth reads
  `BETTER_AUTH_SECRET`/`BETTER_AUTH_URL` from the environment itself. The Microsoft
  Entra ID social provider is wired env-driven
  (`MICROSOFT_CLIENT_ID`/`_SECRET`/`_TENANT_ID`), gated on both client id +
  secret, with `tenantId` required at boot when Microsoft is set (fails loud
  rather than falling back to Entra's open `common` endpoint). Sessions are
  read in server components through the `getUser()` seam in
  `apps/web/src/lib/session.ts`. **Adapter / table separation (deviates from
  `effectivly`, by necessity):** `effectivly` uses Better Auth's built-in
  Kysely adapter over a node-postgres `Pool` and lets Better Auth's own
  migrator own the auth tables — deliberately *not* `@better-auth/drizzle-adapter`,
  which peers on `drizzle-orm ^0.45` while this repo (like `effectivly`) runs
  the `1.0.0-rc` line. We keep that same adapter choice (built-in Kysely, own
  migrator) for the same version reason, BUT the auth tables live in a
  **separate database** from the warehouse domain: a Turso/libSQL database
  pointed at by `AUTH_DATABASE_URL` + `AUTH_DATABASE_AUTH_TOKEN` (local dev
  falls back to a separate file `../../.dev-data/auth.db`). Auth tables
  (user/session/account/verification) must never pollute `packages/domain`'s
  schema — that schema is the warehouse domain synced to phones, and the
  field app must never see auth tables; putting auth in the same Turso
  database the phones sync would also violate the multi-writer discipline
  below (the web app would become a second writer to the sync DB). Better
  Auth's schema/migrations are therefore kept inside `apps/web`
  (the root `auth.ts` CLI-discoverable entrypoint + the `@better-auth/cli`
  `generate`/`migrate` scripts), run against the auth database only. Same requirements as the original
  step 4: session required for everything except `/tag/*`, `/api/health`, and
  auth routes (enforced by `apps/web/src/middleware.ts`); signed-in
  name/email prefill the checkout form; a dev bypass
  (`AUTH_DEV_BYPASS=1`) guarded by `NODE_ENV !== "production"` on the same
  code path. Remove the unused `next-auth` dependency from
  `apps/web/package.json` when wiring it.
- **No un-aliased builder joins in `packages/domain` (2026-07-22)**: the field
  app's RN driver adapter (`apps/field/src/db/drizzleTursoRnDriver.ts`) derives
  Drizzle's array-mode rows from object rows, so a builder join selecting
  duplicate column names (two `id`s) would collapse and misalign columns. Any
  builder join must use an explicit `.select({...})` projection with unique
  aliases; raw `sql` aggregates with named result columns are unaffected.
- **React Native Reusables + NativeWind is the field-app UI standard
  (2026-07-23)**: `apps/field` screens are styled with NativeWind (Tailwind)
  classes and the vendored React Native Reusables (shadcn/ui-style) components
  under `apps/field/src/components/ui/`, with theme CSS variables in
  `apps/field/global.css` (light default; dark wired, no toggle). New field-app
  UI must use these components + tokens rather than ad-hoc `StyleSheet` objects
  (keep `StyleSheet` only for genuinely computed numeric styles, e.g. the
  finder proximity-bar width/height).

## Considered and rejected

- **Wrapping TSL's official iOS SDK (XCFramework)**: the SDK download sits
  behind a tsl.com login and an Apple PPID process. Plan 003 instead speaks
  raw TSL ASCII 2.0 over an External Accessory session — `reader.py` is a
  complete, battle-tested spec of that protocol. Revisit the SDK only if EA
  session behavior proves unreliable.
- **Expo web output instead of Next.js for the jobsite site**: the jobsite site
  is form-and-table pages with server-side auth; Next.js on Vercel is the
  boring, well-documented path.
- **Porting the snapshot/watermark sync protocol to TS**: kept only as plan
  010's escape hatch if Turso sync fails validation; Turso replaces it.
