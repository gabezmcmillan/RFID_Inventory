# Implementation Plans — Expo Rewrite of the RFID Inventory System

Generated by the improve skill (plan variant) on 2026-07-22, at commit `79443fb`.
Last reconciled for Plan 010 on 2026-07-23, at commit `d73717b`.

## What is being built

A ground-up replacement of the warehouse PC app (`apps/warehouse/`, Python/FastAPI/
PyInstaller) and the Vercel cloud mirror (`apps/cloud/`) with:

- **`apps/field/`** — an Expo (React Native) iPhone app. The phone clips to the
  Vulcan RFID Indium sled (Bluetooth, TSL ASCII 2.0 protocol) and replaces the
  warehouse PC entirely: Check In, Check Out, Sweep & Count, Find a Tag,
  Warehouse browse, Requests. BOL capture uses the phone camera instead of the
  Epson ES-50. Labels still print to the Zebra ZD621R over TCP/9100.
- **`apps/web/`** — a Next.js app on Vercel replacing `apps/cloud/`: jobsite
  stock browsing + cart requests + order status + tag QR pages, plus an
  office-facing warehouse browse, behind auth.
- **`packages/domain/`** — a TypeScript package holding the SQL schema, domain
  constants (ported from `apps/warehouse/config.py`), and every repository
  function (ported from `apps/warehouse/db.py`). It runs against a minimal
  `SqlDatabase` interface so the same code runs on-device (Turso React Native),
  and in Node tests.
- **Data**: one Turso warehouse database plus a separate Turso/libSQL Better
  Auth database that is never exposed or synced to phones. The field app uses
  `@tursodatabase/sync-react-native` (local-first reads/writes, `push()`/`pull()`);
  the web app reads/writes the warehouse database over Turso's remote driver.
  This replaces the custom Python sync layer after production acceptance.

The existing Python apps stay untouched as behavioral/hardware references until
Plan 010 acceptance and launch stabilization pass, then move mechanically to
`archive/legacy-python/` in a separate final commit. Deterministic plan checks
remain runnable without hardware or production credentials; Plan 010 launch
also requires explicit production-resource and real-hardware acceptance.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Scaffold the TypeScript monorepo (field app, web app, domain package) | P1 | S | — | DONE |
| 002 | Domain package: schema, constants, repositories, importer | P1 | L | 001 | DONE |
| 003 | Reader module: TSL ASCII 2.0 over Bluetooth + simulated reader | P1 | L | 001 | DONE |
| 004 | Check In (scan path): arm shipment, trigger intake, amend, vendors, notes | P1 | M | 002, 003 | DONE |
| 005 | Label printing: ZPL over TCP, EPC minting, print-path intake | P1 | M | 004 | DONE |
| 006 | Check Out, Sweep & Count, Warehouse browse, Find a Tag, Admin | P1 | L | 004 | DONE |
| 007 | BOL capture with the camera + OCR field extraction | P1 | L | 004 | DONE |
| 008 | Requests: badge, staging, fulfill, decline | P1 | M | 006 | DONE |
| 009 | Web app: jobsite stock, cart, orders, tag pages, office browse, auth | P1 | L | 002 | IN PROGRESS — Better Auth/device-link foundation implemented; final plan acceptance not recorded |
| 010 | Launch secure field sync and production apps | P1 | M (7–12 engineering days if credential gate passes; otherwise STOP/re-plan) | 009; 011 phone check before local acceptance | TODO |
| 011 | Vercel-level local DX: easy Tailscale setup/doctor for the field API | P1 | S | — (dev infra verified by `9d5dbe9`) | IN PROGRESS — setup implemented; iPhone confirmation pending |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Dependency notes

- **002 before every feature plan**: all repositories and types live in
  `packages/domain`; UI plans only wire screens to them.
- **003 before 004**: check-in consumes reader events. 004 is testable with the
  simulated reader that 003 ships, so hardware is never a blocker.
- **004 before 005/006/007**: those plans extend intake/checkout flows and the
  check-in screen that 004 creates.
- **006 before 008**: request fulfillment reuses the checkout staging flow.
- **009 only needs 002** (repositories + schema); it can run in parallel with
  003–008.
- **010's deterministic work may start before launch dependencies finish**:
  its 1–2 day credential spike, field-created ID migration, and focused tests
  may proceed while Plan 009 is completed. Production linking, deployment,
  hardware acceptance, and launch still require Plan 009's web/auth behavior.
- **011's isolated development infrastructure dependency is satisfied**:
  commit `9d5dbe9` provisioned and connected Vercel Marketplace-managed
  `rfid-warehouse-dev` + `rfid-auth-dev` at Development scope, mapped the six
  app-facing env names, applied warehouse/auth migrations, and verified 9/4
  tables through app drivers. Plan 011 reuses
  `scripts/setup-dev-vercel.sh`; direct Turso platform login/provisioning is not
  used. Plan 011's scope is now only easy Tailscale setup/doctor for the field
  API (no supervisor, no QR redesign, no new deps); it does not duplicate Plan
  010's production sync/launch work. Its pending physical-iPhone reachability
  check must pass before Plan 010's local phone-to-Mac API acceptance.

## Standing decisions (do not re-litigate in individual plans)

- **Fresh empty launch; no legacy data migration (2026-07-23, operator
  decision)**: there is no legacy/production database data to migrate.
  Plan 010 reuses and verifies the existing migrated, empty Production
  warehouse database, then creates only explicit smoke inventory through the
  new app. Importer
  development/rehearsal, Python row-count reconciliation, dual-write, and
  legacy-data cutover are rejected. Existing importer code is not a production
  launch dependency and must not be extended or run for production.
- **Existing production resources are reused**: Vercel project
  `rfid-inventory-web` already has separate Production/Preview warehouse and
  auth Turso env pairs. They are direct encrypted envs; `main` is Production
  and `rewrite/expo` is the current Preview. Only Development uses Marketplace-
  managed Turso resources; converting or recreating Production/Preview
  resources is not a launch requirement.
- **Proportional multi-replica safety**: field-created integer IDs become
  globally unique and device identity/EPC serial state moves out of synced
  `local_meta`. Turso's same-row last-push-wins behavior is documented and
  constrained by the existing writer discipline; no generalized operation
  ledger/conflict platform is a launch blocker without failing two-replica
  evidence.
- **Multi-writer discipline**: the field app writes tags/events/vendors/
  notes/bol_docs and *updates* requests; the web app only *inserts* requests.
  Keep those responsibilities, but do not claim they eliminate field-vs-field
  or request update conflicts.
- **EPC minting becomes device-safe**: EPC = 8-hex prefix (`42473031`) +
  2-hex device id + 14-hex per-device serial (was: 16-hex global serial).
  The server permanently assigns each device byte without reusing revoked
  assignments, and the phone reserves serials atomically in local-only state so
  two phones minting offline cannot collide or reuse a serial after a crash.
- **BOL documents are images/PDFs in blob storage** (upload queued while
  offline); the `bol_docs` row keeps `filename` and gains `storage_url`.
- **Everything testable in Node**: `packages/domain` never imports React
  Native. On-device it receives the Turso RN connection; in tests it receives
  `@tursodatabase/database` (Node).
- **No static mobile database secret**: a broad/static
  `TURSO_AUTH_TOKEN` in an Expo bundle is extractable and prohibited. Direct
  sync requires a 1–2 day Plan 010 spike proving server-minted, short-lived,
  least-privilege, revocable per-device credentials and dynamic RN-client
  refresh. If the gate fails, Plan 010 stops for a separate server-mediated
  sync plan; never hand-roll JWT/crypto or ship a static write token.
- **Separate Better Auth database remains mandatory**: the auth database,
  credentials, users, sessions, roles, and device registry never sync to the
  field app.
- **Tailscale is development-only**: Plan 011 provides private local phone-to-
  Mac reachability; production field builds use the validated Vercel HTTPS
  origin and contain no Tailscale dependency.
- **Archive only after acceptance**: `apps/warehouse`, `apps/cloud`, and
  `packages/contract` remain behavioral/hardware references through Plan 010
  acceptance and launch stabilization, then move to
  `archive/legacy-python/` in a separate mechanical commit with active
  references reconciled.
- **Drizzle ORM is the data-access standard (2026-07-22)**: `packages/domain`
  uses `drizzle-orm@rc` / `drizzle-kit@rc` over the Turso drivers. The schema is
  Drizzle `sqliteTable` definitions (source of truth), row types are inferred,
  and migrations are generated by `drizzle-kit` and applied by Drizzle's
  migrator. Plans 003–010 executors must use Drizzle patterns (builder API for
  reads/writes, `db.run(sql\`...\`)` for raw statements, and the `sql` template
  operator with typed result mapping for complex aggregates) — no hand-rolled
  SQL seam.
- **Auth: Better Auth, not Auth.js/NextAuth (2026-07-22, operator decision;
  concretized 2026-07-23 from the `effectivly` reference repo)**: plan 009
  implementation now uses [Better Auth](https://better-auth.com) `1.6.18`
  (same version as `effectivly`), following that repo's house style:
  the server instance lives in `apps/web/src/lib/auth.ts` (a `createAuth()`
  factory over `betterAuth()`), mounted as a catch-all at
  `app/api/auth/[...all]/route.ts` via `toNextJsHandler` from
  `better-auth/next-js`; the browser client is `createAuthClient()` from
  `better-auth/react` in `apps/web/src/lib/auth-client.ts`. Per the Better Auth
  guide, `secret`/`baseURL` are not set in config — Better Auth reads
  `BETTER_AUTH_SECRET`/`BETTER_AUTH_URL` from the environment itself. The Microsoft
  Entra ID social provider is wired env-driven
  (`MICROSOFT_CLIENT_ID`/`_SECRET`/`_TENANT_ID`), gated on both client id +
  secret, with `tenantId` required at boot when Microsoft is set (fails loud
  rather than falling back to Entra's open `common` endpoint). Sessions are
  read in server components through the `getUser()` seam in
  `apps/web/src/lib/session.ts`. **Adapter / table separation (deviates from
  `effectivly`, by necessity):** `effectivly` uses Better Auth's built-in
  Kysely adapter over a node-postgres `Pool` and lets Better Auth's own
  migrator own the auth tables — deliberately *not* `@better-auth/drizzle-adapter`,
  which peers on `drizzle-orm ^0.45` while this repo (like `effectivly`) runs
  the `1.0.0-rc` line. We keep that same adapter choice (built-in Kysely, own
  migrator) for the same version reason, BUT the auth tables live in a
  **separate database** from the warehouse domain: a Turso/libSQL database
  pointed at by `AUTH_DATABASE_URL` + `AUTH_DATABASE_AUTH_TOKEN` (local dev
  falls back to a separate file `../../.dev-data/auth.db`). Auth tables
  (user/session/account/verification) must never pollute `packages/domain`'s
  schema — that schema is the warehouse domain synced to phones, and the
  field app must never see auth tables; putting auth in the same Turso
  database the phones sync would also violate the multi-writer discipline
  below (the web app would become a second writer to the sync DB). Better
  Auth's schema/migrations are therefore kept inside `apps/web`
  (the root `auth.ts` CLI-discoverable entrypoint + the `@better-auth/cli`
  `generate`/`migrate` scripts), run against the auth database only. Sessions
  are required for everything except `/tag/*`, `/api/health`, and auth routes
  (enforced by `apps/web/src/proxy.ts`); signed-in
  name/email prefill the checkout form; a dev bypass
  (`AUTH_DEV_BYPASS=1`) guarded by `NODE_ENV !== "production"` on the same code
  path. Next.js 16 uses the renamed `proxy.ts` convention, and `next-auth` is
  not a dependency.
- **No un-aliased builder joins in `packages/domain` (2026-07-22)**: the field
  app's RN driver adapter (`apps/field/src/db/drizzleTursoRnDriver.ts`) derives
  Drizzle's array-mode rows from object rows, so a builder join selecting
  duplicate column names (two `id`s) would collapse and misalign columns. Any
  builder join must use an explicit `.select({...})` projection with unique
  aliases; raw `sql` aggregates with named result columns are unaffected.
- **React Native Reusables + NativeWind is the field-app UI standard
  (2026-07-23)**: `apps/field` screens are styled with NativeWind (Tailwind)
  classes and the vendored React Native Reusables (shadcn/ui-style) components
  under `apps/field/src/components/ui/`, with theme CSS variables in
  `apps/field/global.css` (light default; dark wired, no toggle). New field-app
  UI must use these components + tokens rather than ad-hoc `StyleSheet` objects
  (keep `StyleSheet` only for genuinely computed numeric styles, e.g. the
  finder proximity-bar width/height).
- **shadcn/ui is the web-app UI standard (2026-07-23)**: `apps/web` is styled
  with Tailwind CSS v4 (`@tailwindcss/postcss`) and shadcn/ui components under
  `apps/web/src/components/ui/`, initialized via the shadcn CLI (`shadcn init`,
  `base-nova` preset — the modern successor to the legacy "new-york" style —
  with CSS-variable theming). Theme tokens live in `apps/web/src/app/globals.css`
  as oklch CSS variables (light default; dark driven by a system-preference
  `dark` class set pre-hydration). The domain status palette is mirrored from
  the field app as HSL tokens `--status-in` (green) / `--status-partial`
  (amber) / `--status-delivered` (gray), exposed to Tailwind as
  `--color-status-*` so `bg-status-in` / `text-status-partial` etc. work. New
  web-app UI must use these shadcn components + tokens rather than hand-rolled
  CSS; pages stay Server Components where they already are, with `"use client"`
  only on genuinely interactive islands (cart, sign-in form, user menu,
  focus-refresh).   The shadcn CLI's `base-nova` preset builds on `@base-ui/react`
  (not Radix), so component APIs use the `render` prop / `useRender` pattern
  rather than `asChild`.
- **Typed, schema-validated env for `apps/web` (2026-07-23)**: all
  `process.env` reads in `apps/web/src` go through a single edge-safe zod
  schema in `apps/web/src/lib/env.ts` (mirrors the `effectivly` `AppConfig`
  house style — one declared schema, raw `process.env` anywhere else is an
  anti-pattern — but zod instead of Effect `Config`, since this repo has no
  Effect dependency). The schema parses once at boot and throws one clear
  message listing every problem. Server vs client (`NEXT_PUBLIC_*`) vars are
  separated. The offline gate is unchanged: `BETTER_AUTH_SECRET` stays optional
  (absent = no auth backend); the only required vars are conditional
  (`BETTER_AUTH_URL` with the secret; `MICROSOFT_TENANT_ID` with both Entra
  client id+secret), so the dev-bypass path still works with zero env. The env
  module is edge-safe (zod + `process.env` only) so the proxy and `dev-bypass`
  can import it without dragging in Node-only `auth.ts`/`db.ts`. `apps/field`
  now has its own single typed seam in `apps/field/src/config/env.ts` for the
  public `EXPO_PUBLIC_DEFAULT_SERVER_URL`; secrets must never be added to
  `EXPO_PUBLIC_*`.
- **Full UI component sets vendored for both apps (2026-07-23)**: every shadcn
  component is vendored under `apps/web/src/components/ui/` (60 files, via
  `shadcn add --all` against the `base-nova` preset) and every React Native
  Reusables component under `apps/field/src/components/ui/` (32 files, via
  `@react-native-reusables/cli add --all`). AI agents should never need to
  install another UI component — everything is available offline. Generated
  files are left unedited; peer deps added in single installs.
- **QR one-time-code device linking (2026-07-23)**: mobile auth uses Better
  Auth's shipped `oneTimeToken` plugin (single-use, 5-min expiry,
  `disableSetSessionCookie` so the phone — no cookie jar — receives the
  session `token` in the verify response body) plus the `bearer` plugin (the
  phone sends `Authorization: Bearer <session.token>` on future requests).
  Flow: a signed-in web user opens `/link-device` (listed in the user menu),
  the server mints a one-time token rendered as a QR (`qrcode.react`); the
  field app's Settings → Link device screen scans it with `expo-camera`
  `CameraView`, exchanges it at `/api/auth/one-time-token/verify`, and stores
  the returned session token + identity in `expo-secure-store` (the
  `apps/field/src/auth/` module exposes the stored credential for future
  sync). The web server URL comes from the typed public default and remains
  configurable for development; Plan 010 locks production builds to the
  validated Vercel HTTPS origin.
  No hand-rolled token crypto — only shipped plugins. Existing web auth
  (proxy gating, dev bypass, Entra sign-in) is unchanged in behavior.
  `expo-camera` is a new native module, so the field app needs a native
  rebuild; the full end-to-end loop requires a live auth backend
  (`BETTER_AUTH_SECRET` + a sign-in method), so the dev-bypass smoke only
  exercises the page render.
- **Local-development DX target (Plan 011; IN PROGRESS, 2026-07-23)**: scope was
  radically narrowed from the original design. The delivered piece is **only** an
  easy, dependency-free way to confirm Tailscale is set up so a physical iPhone
  can reach the local web app. `pnpm tailscale:setup` runs `tailscale serve --bg
  http://127.0.0.1:3000` and prints the Field API URL
  (`https://<mac>.<tailnet>.ts.net`); `pnpm tailscale:doctor` is a read-only
  PASS/WARN/FAIL verifier (CLI/login, Serve→localhost:3000, Funnel off, local +
  tailnet `/api/health`). Both live under `scripts/tailscale/` (one `.mjs` CLI +
  pure parsers, tested with Node's built-in `node:test`, **no npm dependencies**).
  Web/SSO stays `http://localhost:3000`; the Field API origin is the Tailscale
  HTTPS URL the operator pastes into the field app's Settings → Web server URL.
  **Explicitly out of scope (dropped from the original 011 design):** no
  `pnpm dev` supervisor, no `dev:bypass`/`dev:native`/`dev:smoke` profiles, no QR
  origin-handoff redesign, no `FIELD_DEVICE_API_ORIGIN` env schema, no shared
  `@rfid/device-link-protocol` package, no CI expansion, no new runtime/dev deps,
  no auth/UI changes, no ngrok/Cloudflare fallback. Funnel is intentionally not
  used (private tailnet only). Development DB setup remains Vercel
  Marketplace-only via `scripts/setup-dev-vercel.sh`; no direct Turso
  login/provisioning. Expo/Metro remains a separate transport. Status is
  IN PROGRESS because the deterministic setup/doctor + tests pass but the
  physical iPhone reachability check has not been performed.

## Considered and rejected

- **Wrapping TSL's official iOS SDK (XCFramework)**: the SDK download sits
  behind a tsl.com login and an Apple PPID process. Plan 003 instead speaks
  raw TSL ASCII 2.0 over an External Accessory session — `reader.py` is a
  complete, battle-tested spec of that protocol. Revisit the SDK only if EA
  session behavior proves unreliable.
- **Expo web output instead of Next.js for the jobsite site**: the jobsite site
  is form-and-table pages with server-side auth; Next.js on Vercel is the
  boring, well-documented path.
- **Porting the snapshot/watermark sync protocol to TS**: kept only as plan
  history, not Plan 010's fallback. If secure direct Turso sync fails its gate,
  stop and plan a server-mediated transport separately; do not port the Python
  protocol or improvise it inside Plan 010.
