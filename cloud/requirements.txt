# Cloud app (App Service, Linux/Python) — see cloud/README.md for deploy steps.
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
# psycopg v3 with bundled libpq binaries (no system packages needed).
psycopg[binary]>=3.1
